package com.example.belajarspek.external.helper

class Calculator {
    fun add(number1: Int, number2: Int): Int {
        return number1.plus(number2)
    }

    fun subtract(number1: Int, number2: Int): Int {
        return number1.minus(number2)
    }
}